const ValidationValue = {
    minLength: 6,
    maxLength: 18
}

const Status = {
    IDLE: 'Not started',
    PENDING: 'In proccess',
    SUCCESS: 'Ready',
    ERROR: 'Error occured'
}

export { ValidationValue, Status }
